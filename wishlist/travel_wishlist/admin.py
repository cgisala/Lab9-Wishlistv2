from django.contrib import admin
from .models import Place

# Register your models here.

"""Django's admin console can be used to view and manipulate data in the database"""

admin.site.register(Place)